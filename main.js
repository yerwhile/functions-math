const add = (firstNumber, secondNumber) => {
    const sum = firstNumber + secondNumber;
    return sum;
}

const doubleNum = (num) => {
    const doubled = num * 2;
    return doubled;
}

addSum = add(4,14);
multDouble = doubleNum(addSum);
console.log(multDouble);